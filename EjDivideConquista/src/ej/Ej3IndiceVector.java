package ej;

public class Ej3IndiceVector {
/*	Sea A[1..n], n ≥ 1, un vector de enteros diferentes y ordenados crecientemente, tal que
	algunos de los valores pueden ser negativos. Diseñar un algoritmo que devuelva un
	índice natural k, 1 ≤ k ≤ n, tal que A[k] = k, siempre que tal índice exista.
*/
	
	public static int buscarElemento(int[] array, int indice, int inicio, int fin) {
		
		if(indice <= array.length) {
			//Como puedo ir actualizando el indice del array si voy diviendo por 2 el array?
			int mitad = (inicio+fin)/2;
			
			if(indice > mitad) {
				return 
			}
		}
	}
	
}
